# Core Libraries for Data Handling and Pipeline Management
pandas==2.2.2
pyarrow==16.1.0
requests==2.32.3
rich==13.7.1

# Bioinformatics, Statistics, and Machine Learning
scikit-learn==1.5.0
lifelines==0.28.0
statsmodels==0.14.2
matplotlib==3.9.0
seaborn==0.13.2

# NLP for Knowledge Extraction from PDFs
pdfplumber==2024.4.24
spacy==3.7.5
# The scispaCy model must be installed separately (see instructions)

# R Bridge for advanced bioinformatics packages (requires local R installation)
rpy2==3.5.16

pip install "dask[dataframe]"
pip install "dask[distributed]"
pip install "dask[complete]"
